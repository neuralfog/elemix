import { Attributes } from './constants';
import type { AttributeHole } from './holes/attributes/AttributeHole';
export type AttributeDefinition = {
    index: number;
    name: string;
    value: string;
    virtual: boolean;
    type: Attributes;
};
export declare const detectAttributes: (strings: string, index: number) => AttributeDefinition | undefined;
export declare const processAttribute: (fragment: DocumentFragment, definition: AttributeDefinition) => AttributeHole | undefined;
