import type { Hole } from './Hole';
import type { HtmlTemplate } from '../HtmlTemplate';
export declare class StringHole implements Hole {
    commentNode: Comment;
    node: Text;
    constructor(commentNode: Comment);
    setValue(value: HtmlTemplate[]): void;
}
