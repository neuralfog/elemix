import type { Hole } from './Hole';
import type { HtmlTemplate } from '../HtmlTemplate';
export declare class ListHole implements Hole {
    commentNode: Comment;
    private renderer;
    constructor(commentNode: Comment);
    setValue(values: HtmlTemplate[]): void;
}
