import type { AttributeDefinition } from '../../attributes';
import type { AttributeHole } from './AttributeHole';
export declare class PropHole implements AttributeHole {
    node: HTMLElement;
    definition: AttributeDefinition;
    constructor(node: HTMLElement, definition: AttributeDefinition);
    setValue(value: unknown): void;
}
