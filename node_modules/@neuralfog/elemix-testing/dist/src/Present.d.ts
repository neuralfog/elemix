import type { Template } from './types';
export declare class Present {
    private rootElement;
    screen(template: Template): this;
    root<Component extends HTMLElement>(): Component;
    wait(timeout?: number): Promise<void>;
    getByTag<Component extends HTMLElement>(tag: string): Component;
    private get;
}
